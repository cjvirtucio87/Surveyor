<% range = (1..number_range_question.max).to_a %>
<%= content_tag :h3, number_range_question.description %>
<% range.each do |num| %>
  <%= label :range, num %>
  <%= radio_button_tag :range, num %>
<% end %>
